import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './smart_document_analyzer.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);